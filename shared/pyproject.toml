[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "jarvis-shared"
version = "0.1.0"
description = "Shared types and schemas for JARVIS services"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.8.0",
]
